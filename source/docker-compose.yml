version: '3.1'

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust
      POSTGRES_USER: iliakruchinin
      POSTGRES_DB: mydatabase

  api:
    build: .
    restart: always
    ports:
      - 5000:5000
    depends_on:
      - db
